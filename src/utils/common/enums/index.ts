export enum Role {
  user = "user",
  admin = "admin",
}
export enum Agent {
  local = "local",
  google = "google",
}

export enum Gender {
  male = "male",
  female = "female",
}
export enum TokenType {
  access = "access",
  refresh = "refresh",
}
export enum StatusOrder {
  confirmed = "confirmed",
  pending = "pending",
  canalled = "cancelled",
}
export enum Payment {
  cash = "cash",
  card = "card",
}